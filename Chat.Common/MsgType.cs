namespace Chat.Common
{
    /// <summary>
    /// 消息类型
    /// </summary>
    public enum MsgType
    {
        NONE,
        JOIN_RESULT,
        NEW_JOIN,
        QUIT,
        SEND_RESULT,
        MSG
    }
}
